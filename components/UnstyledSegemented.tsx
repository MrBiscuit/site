// This is a skeleton starter React component generated by Plasmic.
// This file is owned by you, feel free to edit as you see fit.
import * as React from "react";
import {
  PlasmicUnstyledSegemented,
  DefaultUnstyledSegementedProps
} from "./plasmic/mr_biscuit_site/PlasmicUnstyledSegemented";
import { HTMLElementRefOf } from "@plasmicapp/react-web";

// Your component props start with props for variants and slots you defined
// in Plasmic, but you can add more here, like event handlers that you can
// attach to named nodes in your component.
//
// If you don't want to expose certain variants or slots as a prop, you can use
// Omit to hide them:
//
// interface UnstyledSegementedProps extends Omit<DefaultUnstyledSegementedProps, "hideProps1"|"hideProp2"> {
//   // etc.
// }
//
// You can also stop extending from DefaultUnstyledSegementedProps altogether and have
// total control over the props for your component.
export interface UnstyledSegementedProps
  extends DefaultUnstyledSegementedProps {}

function UnstyledSegemented_(
  props: UnstyledSegementedProps,
  ref: HTMLElementRefOf<"div">
) {
  const [current, setCurrent] = React.useState(0);

  return <PlasmicUnstyledSegemented root={{ ref }} 
  first={{
    props:{
      onClick: () => setCurrent(0),
      isCurrent: current === 0
     }
  }}
  second={{
    props:{
      onClick: () => setCurrent(1),
      isCurrent: current === 1
      }
  }}  
  third={{
    props:{
      onClick: () => setCurrent(2),
      isCurrent: current === 2
      }
  }}
  
  
  {...props} />;
}

const UnstyledSegemented = React.forwardRef(UnstyledSegemented_);
export default UnstyledSegemented;
